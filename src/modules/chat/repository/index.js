/* eslint-disable */
// Owned tables: messages, attachments (meta only)

/** TODO: Replace with real DB query. */
function listMessages(hikeId) { void hikeId; return Promise.resolve([]); }

/** TODO: Replace with real DB insert. */
function saveMessage(message) { void message; return Promise.resolve({ id: 'm1' }); }

module.exports = { listMessages, saveMessage };
