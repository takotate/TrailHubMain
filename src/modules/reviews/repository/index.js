/* eslint-disable */
// Owned tables: reviews

/** TODO: Replace with real DB insert. */
function createReview(data) { void data; return Promise.resolve({ id: 'r1' }); }

/** TODO: Replace with real DB query. */
function listReviewsByGuide(guideId) { void guideId; return Promise.resolve([]); }

/** TODO: Replace with real DB query. */
function listReviewsByHike(hikeId) { void hikeId; return Promise.resolve([]); }

module.exports = { createReview, listReviewsByGuide, listReviewsByHike };
