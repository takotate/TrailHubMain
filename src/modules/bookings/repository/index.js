/* eslint-disable */
// Owned tables: bookings, participants, payment_intents

/** TODO: Replace with real DB insert. */
function createBooking(data) { void data; return Promise.resolve({ id: 'b1' }); }

/** TODO: Replace with real DB delete. */
function deleteBooking(id) { void id; return Promise.resolve(true); }

module.exports = { createBooking, deleteBooking };
